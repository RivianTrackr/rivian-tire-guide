<?xml version="1.0"?>
<ruleset name="Rivian Tire Guide">
    <description>WordPress Coding Standards for the Rivian Tire Guide plugin.</description>

    <!-- Scan these files/directories -->
    <file>rivian-tire-guide.php</file>
    <file>uninstall.php</file>
    <file>includes/</file>
    <file>admin/</file>

    <!-- Exclude third-party and build artifacts -->
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/*.min.js</exclude-pattern>
    <exclude-pattern>*/*.min.css</exclude-pattern>
    <exclude-pattern>*/tests/*</exclude-pattern>

    <!-- Use WordPress Coding Standards -->
    <rule ref="WordPress">
        <!-- Allow short array syntax ([] instead of array()). -->
        <exclude name="Generic.Arrays.DisallowShortArraySyntax"/>

        <!-- The plugin uses static class methods extensively — allow it. -->
        <exclude name="Universal.Files.SeparateFunctionsFromOO"/>

        <!-- Allow file-level phpcs:disable for specific rules. -->
        <exclude name="Squiz.Commenting.FileComment.Missing"/>

        <!-- Allow inline SQL — the plugin uses $wpdb->prepare() correctly. -->
        <exclude name="WordPress.DB.DirectDatabaseQuery.DirectQuery"/>
        <exclude name="WordPress.DB.DirectDatabaseQuery.NoCaching"/>

        <!--
            House formatting style. This codebase indents with four spaces
            rather than tabs, does not align assignments or array arrows into
            columns, and keeps short function calls on one line. Those are
            deliberate and consistent throughout; left enabled they produce
            over 11,000 findings that bury everything worth reading.
        -->
        <exclude name="Generic.WhiteSpace.DisallowSpaceIndent"/>
        <exclude name="Generic.WhiteSpace.ScopeIndent"/>
        <exclude name="Universal.WhiteSpace.PrecisionAlignment"/>
        <exclude name="Generic.Formatting.MultipleStatementAlignment"/>
        <exclude name="WordPress.Arrays.MultipleStatementAlignment"/>
        <exclude name="WordPress.Arrays.ArrayDeclarationSpacing"/>
        <exclude name="WordPress.WhiteSpace.OperatorSpacing"/>
        <exclude name="PEAR.Functions.FunctionCallSignature"/>
        <exclude name="Squiz.Functions.MultiLineFunctionDeclaration"/>
        <exclude name="Squiz.PHP.EmbeddedPhp"/>

        <!--
            Syntax preferences the plugin consistently does the other way:
            conditions are written subject-first, short ternaries and
            standalone post-increment are used freely.
        -->
        <exclude name="WordPress.PHP.YodaConditions"/>
        <exclude name="Universal.Operators.DisallowShortTernary"/>
        <exclude name="Universal.Operators.DisallowStandalonePostIncrementDecrement"/>
    </rule>

    <!-- Verify text domain consistency. -->
    <rule ref="WordPress.WP.I18n">
        <properties>
            <property name="text_domain" type="array">
                <element value="rivian-tire-guide"/>
            </property>
        </properties>
    </rule>

    <!-- Minimum supported PHP version. -->
    <config name="minimum_supported_wp_version" value="5.8"/>
    <config name="testVersion" value="7.4-"/>

    <!-- Show progress and use colors. -->
    <arg value="ps"/>
    <arg name="colors"/>

    <!-- Check only PHP files. -->
    <arg name="extensions" value="php"/>
</ruleset>
